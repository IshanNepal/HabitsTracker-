import React from 'react'

const usePostHabit = () => {
    const handleSumbit = async ({title, is_hobby, desc}) => {
        const res = await fetch('/')
    }
  return {

  }
}

export default usePostHabit